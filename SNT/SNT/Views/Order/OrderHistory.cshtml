@model List<SNT.ViewModels.OrderCompleteViewModel>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "OrderHistory";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>OrderHistory</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div id="products" class="mt-3">
    @for (int i = 0; i < Model.Count; i += 5)
    {
        <div class="products-row row d-flex justify-content-around my-5">
            @for (int j = i; j < i + 5; j++)
            {
                if (j < Model.Count)
                {
            <div class="product col-md-2">
                @*<div class="image-holder d-flex justify-content-center">
            <a href="/Tyre/Review/@Model[j].Id">
                <img class="img-thumbnail product-image" src="@Model[j].Picture" alt="...">
            </a>
        </div>*@
                <h5 class="text-primary text-center mt-3">@Model[j].Id</h5>
                <h5 class="text-primary text-center mt-3">@Model[j].ClientName</h5>
                <h5 class="text-primary text-center mt-3">@Model[j].DateOfCreation</h5>
                <h5 class="text-primary text-center mt-3">@Model[j].Sum.ToString()</h5>

                <form asp-action="Review" asp-controller="Order" method="GET">
                    <input type="hidden" value="@Model[j].Id" name="orderId" />
                    <input type="submit" value="View" class="btn btn-primary" />
                </form>
            </div>
                }
                else
                {
                    <div class="product col-md-2 empty">
                    </div>
                }
            }
        </div>
    }
</div>